syntax = "proto3";

package logging;

message Empty {}

service LoggingService {
  rpc LogMessage (LogRequest) returns(Empty);
  rpc GetLogs (Empty) returns (Messages);
}

message LogRequest {
  string uuid = 1;
  string msg = 2;
}

message Messages {
  string messages = 1;
}
